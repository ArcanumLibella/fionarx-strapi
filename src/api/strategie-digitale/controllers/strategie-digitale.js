'use strict';

/**
 * strategie-digitale controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::strategie-digitale.strategie-digitale');
